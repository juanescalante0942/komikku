import Favorites from "../components/Favorites";

export const metadata = {
  title: "Favorites",
  description:
    "All the manga you’ve saved in one convenient place, stored on your browser with no login required.",
};

export default function FavoritesPage() {
  return <Favorites />;
}
